# 人与机器抢答功能

## 功能概述

人与机器抢答是 KET 备考系统的一个游戏化学习模式，让学生与 AI 机器人进行实时答题竞赛，通过竞争机制提升学习趣味性和参与度。

## 功能特点

- 🎮 **实时竞赛**：学生和 AI 同时答题，比拼速度和准确度
- 🤖 **智能 AI**：AI 根据题目难度调整答题速度和正确率，保持游戏趣味性
- 📊 **战绩统计**：记录总场次、胜率、最快答题时间、最长连胜等数据
- 🎯 **多种模式**：支持选择难度（1-5级）、模块（词汇/语法/阅读）、轮数（5/10/20题）
- 🏆 **即时反馈**：每题结束后立即显示双方答案、用时和胜负结果

## 使用方法

### 1. 启动后端服务

```bash
cd main/backend
python main.py
```

后端服务将在 `http://localhost:8000` 启动

### 2. 启动前端服务

```bash
cd main/frontend
npm run dev
```

前端服务将在 `http://localhost:5173` 启动

### 3. 访问抢答模式

1. 登录系统
2. 访问 `/speed-quiz` 路由
3. 选择难度、模块和题目数量
4. 点击"开始抢答"
5. 与 AI 竞速答题

## API 端点

### 开始抢答
```
POST /api/speed-quiz/start
Body: { difficulty, module, rounds }
Response: { battle_id, question }
```

### 提交答案
```
POST /api/speed-quiz/submit
Body: { battle_id, question_id, answer, answer_time }
Response: { is_correct, ai_answer, ai_time, winner, next_question }
```

### 获取战绩
```
GET /api/speed-quiz/stats
Response: { total_battles, wins, losses, win_rate, fastest_time, max_streak }
```

### 获取历史记录
```
GET /api/speed-quiz/history?page=1&page_size=10
Response: { battles, total, page, page_size }
```

## 数据库表

### speed_quiz_battles
- 记录每场抢答的基本信息
- 字段：user_id, difficulty, module, total_questions, user_correct, ai_correct, user_wins, ai_wins

### speed_quiz_details
- 记录每道题的详细信息
- 字段：battle_id, question_id, user_answer, user_time, ai_answer, ai_time, winner

## AI 算法

### 答题时间
- 难度 1-2：5-10秒
- 难度 3：8-15秒
- 难度 4-5：12-20秒
- 随机波动 ±20%

### 正确率
- 难度 1-2：85-95%
- 难度 3：75-85%
- 难度 4-5：60-75%

## 技术栈

**后端**
- Python FastAPI
- SQLAlchemy ORM
- SQLite 数据库

**前端**
- Vue 3 + TypeScript
- Pinia 状态管理
- Tailwind CSS

## 文件结构

```
main/
├── backend/
│   ├── models/
│   │   ├── db.py (新增 SpeedQuizBattle, SpeedQuizDetail)
│   │   └── schema.py (新增抢答相关模型)
│   ├── services/
│   │   └── speed_quiz_service.py (抢答业务逻辑)
│   ├── api/routes/
│   │   └── speed_quiz_router.py (API 路由)
│   └── migrations/
│       └── add_speed_quiz_tables.py (数据库迁移)
├── frontend/
│   ├── pages/
│   │   └── SpeedQuiz.vue (抢答页面)
│   ├── stores/
│   │   └── speedQuizStore.ts (状态管理)
│   └── services/
│       └── speedQuizService.ts (API 调用)
└── docs/
    ├── prd/
    │   └── speed-quiz-battle.md (产品需求文档)
    └── tech_designs/
        └── speed-quiz-battle.md (技术设计文档)
```

## 开发者

本功能由 Claude Dev Team AI System 开发完成。

🤖 Generated by Claude Dev Team AI System
